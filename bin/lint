#!/usr/bin/env bash
set -e

echo "Running linters with auto-fix..."

bundle exec rubocop -a

yarn prettier --write '**/*.{js,ts,jsx,tsx,css,scss}'

bundle exec erb_lint --lint-all --autocorrect

echo "Linting & Fixing complete!"
