<% title "サーバーエラー (500)" %>
<% noindex %>

<%= render Common::ErrorPage::Component.new(
      code: "500",
      title: "サーバーエラーが発生しました",
      badge_text: "System Error",
      badge_classes: "bg-red-100 text-red-600 border border-red-200 rotate-3"
    ) do %>

  <p>
    予期せぬエラーが発生しました。<br>
    しばらく時間を置いてから再度お試しください。
  </p>

  <div class="mt-8 flex justify-center">
    <%= render Common::Button::Component.new(
          href: root_path,
          variant: :secondary,
          class: "shadow-sm"
        ) do %>
      <%= inline_svg_tag "icons/home.svg", class: "w-5 h-5 mr-2 text-slate-400", aria_hidden: true %>
      ダッシュボードへ戻る
    <% end %>
  </div>

<% end %>
