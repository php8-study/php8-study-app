<% title "ページが見つかりません (404)" %>
<% noindex %>

<%= render Common::ErrorPage::Component.new(
      code: "404",
      title: "ページが見つかりません",
      badge_text: "Exception: NotFound",
      badge_classes: "bg-slate-900 text-white shadow-lg -rotate-6"
    ) do %>

  <p>
    お探しのページは削除されたか、URLが間違っている可能性があります。<br>
    コードの書き間違いではありませんか？
  </p>

  <div class="mt-8 flex justify-center">
    <%= render Common::Button::Component.new(
          href: root_path,
          variant: :primary,
          class: "shadow-md hover:shadow-lg hover:-translate-y-0.5"
        ) do %>
      <%= inline_svg_tag "icons/home.svg", class: "w-5 h-5 mr-2 -ml-1", aria_hidden: true %>
      トップへ戻る
    <% end %>
  </div>

<% end %>
