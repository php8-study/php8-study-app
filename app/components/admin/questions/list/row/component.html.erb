<tr class="hover:bg-slate-50 transition-colors group" id="<%= dom_id(@question) %>">

  <th scope="row" class="whitespace-nowrap py-4 pl-4 pr-3 text-sm font-medium text-slate-900 sm:pl-6 text-left">
    #<%= @question.id %>
  </th>

  <td class="px-3 py-4 text-sm text-slate-600 max-w-xs truncate" title="<%= @question.content %>">
    <%= truncate_content %>
  </td>

  <td class="whitespace-nowrap px-3 py-4 text-sm text-slate-600">
    <span class="inline-flex items-center rounded-md bg-slate-100 px-2 py-1 text-xs font-medium text-slate-600 ring-1 ring-inset ring-slate-500/10">
      <%= category_name %>
    </span>
  </td>

  <td class="whitespace-nowrap px-3 py-4 text-sm text-center">
    <% b = badge(@question.official_page.present?) %>
    <span class="inline-flex items-center rounded-full px-2 py-1 text-xs font-medium ring-1 ring-inset <%= b[:classes] %>">
      <%= b[:text] %>
    </span>
  </td>

  <td class="whitespace-nowrap px-3 py-4 text-sm text-center">
    <% b = badge(@question.explanation.present?) %>
    <span class="inline-flex items-center rounded-full px-2 py-1 text-xs font-medium ring-1 ring-inset <%= b[:classes] %>">
      <%= b[:text] %>
    </span>
  </td>

  <td class="relative whitespace-nowrap py-4 pl-3 pr-4 text-right text-sm font-medium sm:pr-6">
    <div class="flex justify-end gap-2">
      
      <%= render Common::IconButton::Component.new(
            href: edit_admin_question_path(@question),
            icon: "edit",
            variant: :primary,
            method: :get,
            title: "ID:#{@question.id}を編集"
          ) %>

      <%= render Common::IconButton::Component.new(
            href: admin_question_path(@question),
            icon: "trash",
            variant: :danger,
            title: "ID:#{@question.id}を削除",
            method: :delete,
            confirm: "ID:#{@question.id} を本当に削除しますか？"
          ) %>

    </div>
  </td>
</tr>
